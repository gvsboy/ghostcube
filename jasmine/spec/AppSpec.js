describe('App', function() {

  var cube = createCube(),
      players = createPlayers(cube),
      bot = players[1];

  it('exists', function() {
    expect(App).toBeDefined();
  });

  xdescribe('_resetGameState()', function() {

    it('resets all the tiles', function() {

    });

  });

});
